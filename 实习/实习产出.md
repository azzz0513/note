## 开发接口
InternalChangeAuthRequest，BodyDataPermissionResponse，CancelBodyDataPermission，HospitalInfoQuery，AdminRequestBodyDataPermission，AuthorizeAgreement，ChangeDataAuthRequest，DataAuthorizationInfoDetail，DataAuthorizationInfo， WechatRegister，WechatBind，WechatAppURLLink，AttributeAvailableDataSourceSearch，AttributeAvailableUserSearch，AttributeUserDataExport， AttributeUserDataFilterList，AttributeUserData，AttributeUserSituationQuery，UserQuestionnaireEvaluationReportDetail，UserQuestionnaireEvaluationReportList， UserQuestionnaireEvaluationReportPDFFetch，UserQuestionnaireEvaluationReportPDFGenerate，UserQuestionnaireEvaluationReportSummaryEdit，UserQuestionnaireEvaluationReportSummaryTemplateCreate，UserQuestionnaireEvaluationReportSummaryTemplateDelete，UserQuestionnaireEvaluationReportSummaryTemplateEdit，UserQuestionnaireEvaluationReportSummaryTemplateGroupCreate，UserQuestionnaireEvaluationReportSummaryTemplateGroupDelete，UserQuestionnaireEvaluationReportSummaryTemplateGroupList，UserQuestionnaireEvaluationReportSummaryTemplateList，UserQuestionnaireEvaluationReportSummaryTemplateQuery，HumanSettlementReportHeterogeneousDataBatch

## 人体服务
### 版本A：偏业务结果（推荐放在实习经历里）

- 负责医院健康管理系统中“用户属性数据 + 问卷评估报告”模块后端开发，独立交付 20+ 核心接口，覆盖查询、筛选、导出、报告生成、模板管理及批处理全流程。
- 设计并实现评估报告链路（列表/详情/总结编辑/PDF生成与拉取），支持医护端高效查看与分发用户评估结果，提升报告交付效率。
- 搭建报告总结模板与模板分组管理能力（增删改查），将重复性总结内容模板化，提升运营/医生配置效率与内容一致性。
- 实现用户属性数据筛选与导出能力，支撑多维条件检索与数据下游使用，满足业务分析和运营复盘场景。
- 参与人居环境报告异构数据批处理接口开发，提升多来源数据整合能力，增强系统对复杂数据场景的适配性。

### 版本B：偏技术能力（适合“项目经历/技术亮点”）

- 基于 Go 后端完成健康管理业务接口设计与实现，覆盖 RESTful API 规范、参数校验、错误处理与业务逻辑分层。
- 构建问卷评估报告领域模型，打通“数据查询 -> 报告汇总 -> PDF生成/获取 -> 模板管理”闭环流程。
- 设计模板分组与模板 CRUD 机制，沉淀可复用内容配置能力，降低重复开发与人工维护成本。
- 实现导出与批处理场景接口，提升大批量数据处理与跨源数据整合能力。

---

## 你还可以加的“加分信息”（有就写）

简历最能拉开差距的是量化，建议补这些数字（哪怕是区间）：

- 接口数量：`20+`
- 服务对象：如“服务医生/运营/体检用户 XX 人”
- 调用量：如“日均调用 Xk+”
- 性能：如“核心列表接口响应时间从 A ms 降到 B ms”
- 效率：如“报告模板化后，人工编辑时间减少 X%”
- 稳定性：如“线上问题率下降 X% / 无重大事故”

---

## 一段“实习经历”完整示例（可直接改）

XX医院数字健康平台｜后端开发实习生

- 负责用户属性与问卷评估报告模块后端研发，独立完成 `20+` 个核心接口开发，覆盖数据查询、筛选导出、报告详情、总结编辑、PDF生成/获取、模板及分组管理。
- 搭建评估报告模板化能力（模板 CRUD + 分组管理），支撑业务快速配置总结内容，提升报告生产效率与内容标准化水平。
- 实现属性数据筛选与导出、异构数据批处理能力，满足多场景数据分析与批量处理需求。
- 与产品/前端协作推进接口联调与验收，保障需求按期上线并稳定运行。

## 康养用户服务




## 稳态用户服务
## 简历项目描述（可直接用）

项目名称：健康管理平台（Go 后端，实习）  
技术栈：Go、go-zero、MySQL、Redis、HTTP/OpenAPI、OAuth2/Token 体系

- 负责数据授权核心链路开发与维护，覆盖 `AuthorizeAgreement`、`ChangeDataAuthRequest`、`DataAuthorizationInfo`、`DataAuthorizationInfoDetail`，实现授权申请、状态流转、授权列表与详情查询的完整闭环。
- 设计并落地授权状态机（未授权/授权中/已授权/取消），打通本地授权表与中台用户属性双写一致性，补齐历史用户授权数据兜底逻辑，降低状态不一致风险。
- 实现微信用户注册与绑定能力（`WechatRegister`、`WechatBind`），联调中台登录/绑定接口，完善 token 失效自动刷新、异常码透传、登录态缓存写入等关键流程。
- 开发 `WechatAppURLLink` 小程序外链生成接口，支持环境参数兜底与错误分级处理，提升从 H5/浏览器跳转小程序场景的稳定性。
- 在多个接口中补充参数校验、权限上下文校验、日志与错误处理分层，提升接口可观测性与线上问题定位效率。

---

## 更“结果导向”的写法（面试官更爱看）

如果你有数据，可以替换下面的 `X`：

- 独立负责 7 个核心接口迭代，上线后将数据授权相关异常工单下降 `X%`，授权状态不一致问题显著减少。
- 完成微信注册/绑定全链路改造，优化 token 失效重试与刷新机制，第三方接口导致的登录失败率下降 `X%`。
- 通过历史数据兼容与兜底插入逻辑，修复存量用户授权展示异常，覆盖 `X` 类历史场景。

---

## 一句话版本（放“实习职责”也行）

- 在 Go + go-zero 微服务中负责“数据授权 + 微信登录绑定”模块，完成从协议授权、状态流转到第三方中台联调与缓存会话管理的端到端交付。