主从复制是指将一台Redis服务器（主节点（master））的数据复制到其他Redis服务器（从节点（slave））
- 一个主节点可以有多个从节点，而每个从节点只能有一个主节点
- 数据的复制是单向的，只能从主节点复制到从节点，一般主节点负责写操作，从节点负责读操作

## 命令配置
- 查看当前节点角色：`role`
- 配置所属主节点：`replicaof host port`或`slaveof host port`