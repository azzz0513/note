在一次请求中执行多个命令
与关系型数据库不同，Redis的事务不能保证所有的命令都会执行成功，执行结果取决于事务中的命令
保证三点：
- 在执行`EXEC`之前，所有的命令都在一个队列中缓存起来，不会立即执行
- 在收到`EXEC`后，事务开始执行，事务中的任何一个命令执行失败，其他命令依然会执行
- 在事务执行的过程中，其他客户端提交的命令请求，并不会被插入到事务的执行命令序列中


- 开启一个事务（事务开启后，所有的命令都会被放入到一个队列中）：`MULTI`
- 执行事务中的所有命令：`EXEC`