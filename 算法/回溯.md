回溯三问：
1. 当前操作
2. 子问题
3. 下一个子问题

### 全排列
![[images/Pasted image 20251209115714.png]]
数组path记录路径上的数（已选数字）
集合s记录剩余未选数字
回溯三问：
- 当前问题：从s总枚举`path[i]`要填入的数字x
- 子问题：构造排列`>= i`的部分，剩余未选数字集合为s
- 下一个子问题：构造`>= i+1`的部分，剩余未选数字集合为`s - {x}`
![[images/Pasted image 20251209120143.png]]

或者用一个布尔数组onPath记录在path中的数字，如果`nums[i]`在path中，则`onPath[i]`为真
```Go
func permute(nums []int) [][]int {
    ans := [][]int{}
    n := len(nums)
    path := make([]int, n)
    onPath := make([]bool, n)
    var dfs func(int)
    dfs = func(i int) {
        if i == n {
            ans = append(ans, append([]int(nil), path...))
            return
        }

        for j, on := range onPath {
            if !on {
                path[i] = nums[j]
                onPath[j] = true
                dfs(i+1)
                onPath[j] = false
            }
        }
    }
    dfs(0)
    return ans
}
```
时间复杂度分析：
![[images/Pasted image 20251209120623.png]]
对于分叉>=2的树的部分，有：非叶子节点数<叶节点数，所以时间复杂度可以只看叶节点数量

### n皇后


## 子集型回溯
每个元素都可以选/不选
![[images/Pasted image 20251209195537.png]]
![[images/Pasted image 20251209202415.png]]

```Go
func subsets(nums []int) [][]int {
    n := len(nums)
    ans := [][]int{}
    path := make([]int, 0, n)
    var dfs func(int)
    dfs = func(i int) {
        if i == n {
            ans = append(ans, slices.Clone(path))
            return
        }

        dfs(i+1) // 不选当前元素

		// 选择当前元素
        path = append(path, nums[i])
        dfs(i+1) // 因为{1, 2}和{2, 1}重复，所以下一个数一定要大于现在的数
        path = path[:len(path)-1]
    }
    dfs(0)
    return ans

}
```