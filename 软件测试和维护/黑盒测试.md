### 什么是黑盒测试
**Black Box testing** is based entirely on the program specification and aims to verify that the program meets the specified requirements. / **黑盒测试**完全基于程序规范，旨在验证程序是否满足指定要求。

Black-Box testing provides for **coverage of the specification**, but not full coverage of the implementation. That is, there may be code in the implementation that produces results not stated in the specification. / 黑盒测试提供了规范的覆盖，但没有实现的完全覆盖。也就是说，实现中可能存在产生规范中未说明结果的代码。

### 黑盒测试的原则（principle）
![[images/Pasted image 20260525201031.png]]
1. Test **against** the specification（**对照**规范进行测试）
2. Use test coverage criteria based on the specification（基于需求使用测试覆盖准则）
3. Develop test cases derived from the specification （开发根据规范导出的测试用例）
4. "Exercise" the specification （“行使”规范）

![[images/Pasted image 20260525201328.png]]

### Equivalence Partitioning 等价类划分
#### 什么是等价类划分？
An Equivalence Partition (EP) is **a range of values** for a parameter for which the specification states equivalent processing.
定义：等价划分 （EP） 是规范中规定的等效处理的**参数的值范围**。

例子：
```Go
// 判定是否为负
/*
可以划分为两个等价类，分别是
[math.MaxInt, -1]
[0, math.MinInt]
*/
func isNegative(x int) bool {
	return x < 0
}
```
![[images/Pasted image 20260525203009.png]]
![[images/Pasted image 20260525203023.png]]

#### 什么是等价类
![[images/Pasted image 20260525203037.png]]
等价类形成集合的分区。
Partition：相互不相交的子集的集合，其Union是整个集合

#### 等价类的分类
![[images/Pasted image 20260525203123.png]]
**Valid Equivalence Class（有效等价类）**
- 输入域中有意义的一组数据
- 用于验证系统功能和性能是否能被**准确地实施**
- 符合需求规格、程序应该正常接受的数据集合。

**Invalid Equivalence Class（无效等价类）**
- 输入域中无意义的一组数据
- 用于测试系统的**容错性**
- 不符合需求规格、程序应该拒绝的数据集合。

例如：
假设程序要求用户年龄要求：年龄必须在1~120之间
那么：1~120就是一个有效等价类，例如12、23、99都是有效等价类
同时，小于1、大于120、非数字等都属于无效等价类，例如0、123、abc都是无效等价类

#### 识别测试用例的步骤
![[images/Pasted image 20260525203334.png]]
- 根据定义/业务需求识别输入/输出
	- 例：表格/命令行输入/输出信息/计算等
- 根据被定义的输入/输出识别等价类
	- 范围识别
```Go
/*
可以划分为三等价类，其中，一个有效等价类，两个无效等价类
有效等价类
[0, 100]
无效等价类
[math.MinInt, 0)
(100, math.MaxInt]
*/
func isValidScore(x int) bool {
	return x >= 0 && x <= 100
}
```
- 集合识别（有效集合、无效集合）
![[images/Pasted image 20260525203636.png]]

**布尔类型划分等价类**：
- 一个有效等价类（true）
- 一个无效等价类（false）

强制输入划分等价类：
- 一个有效等价类（valid）
- 至少一个无效等价类（null）

符合规则划分等价类：
- 一个有效等价类（符合所有规则）
- 多个无效等价类（违背任意一个规则）

- 将每一个输入划分为等价类，并且形成一张等价类表，为每一个等价类形成一个独特的ID
例 1：百分制评分

| 参数    | 有效等价类             | 无效等价类                |
| ----- | ----------------- | -------------------- |
| Score | 0 <= X <= 100 (1) | X < 0 (2)、X > 100（3） |

- 为有效等价类和无效等价类书写测试用例（有时候非出现overlap重叠的现象）
	- 针对有效等价类设计测试用例
		- 每个测试用例都涵盖尽可能多的有效类，然后重复，直到测试用例集覆盖所有有效类
	- 针对无效等价类设计测试用例
		- 每个测试用例仅涵盖一个无效类（包括一个无效值，其余值都将有效）
![[images/Pasted image 20260525205052.png]]

### 等价类测试
等价类测试：从每一个等价类中使用一个元素
![[images/Pasted image 20260525205125.png]]
弱等价类：只需要 4 个用例即可全覆盖（毕竟是 or 嘛；A, B, C 选最长的那一个即可咯）
`#test cases = #classes in the partition with the largest numbering of subsets`
`#测试用例 = #classes 个子集编号最大的分区`

| Test Case | A   | B   | C   |
| --------- | --- | --- | --- |
| 1         | a1  | b1  | c1  |
| 2         | a2  | b2  | c2  |
| 3         | a3  | b3  | c1  |
| 4         | a1  | b4  | c2  |
强等价类：需要 3 * 4 * 2 = 24 个用例（笛卡尔积）

理解：
假设一个系统有多个输入项：
```
用户名
密码
年龄
```
每个输入项都能划分自己的等价类

弱等价类：每次只考虑“一个输入条件”的等价类变化，其他输入条件默认取正常值
例：注册功能：用户名要求4~10位，密码要求6~12位
弱等价类测试：
用例1：（用户名非法）

| 用户名     | 密码         |
| ------- | ---------- |
| abc（太短） | 123456（正常） |

用例2：（密码非法）

|用户名|密码|
|---|---|
|user123（正常）|123（太短）|

强等价类：要考虑多个输入条件之间的组合情况
也就是不同等价类之间进行组合
用例1：

|用户名|密码|
|---|---|
|abc（非法）|123（非法）|
用例2：

|用户名|密码|
|---|---|
|user123（合法）|123（非法）|

用例3：

|用户名|密码|
|---|---|
|abc（非法）|123456（合法）|

#### 为什么需要有强等价类
因为，现实系统中多个输入组合后，可能会产生新的问题

例如：系统规定：
```
用户名不能为空
密码不能为空
```
单独测试：
- 用户名空
- 密码空
都没有问题
但是，用户名和密码同时位空
程序可能直接：
- 空指针
- 崩溃
- SQL异常

### 总结
- 等价类划分是最低限度的黑盒测试，每一个输入和输出的划分都至少会有一个值会被测试，使用最小数目的测试用例
- 这些测试很可能确保代码的基本数据处理方面是正确的，但它们并没有测试代码中做出的各种决策
- 